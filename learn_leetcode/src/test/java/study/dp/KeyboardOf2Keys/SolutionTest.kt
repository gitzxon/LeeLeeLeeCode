package study.dp.KeyboardOf2Keys

import org.junit.Test
import kotlin.test.assertEquals

class SolutionTest {
    @Test
    fun minSteps() {
        assertEquals(
            3, Solution().minSteps(3)
        )
    }
}